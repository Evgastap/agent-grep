{
  "name": "agent-grep",
  "version": "0.3.0",
  "description": "Interactive grep across Claude Code and Codex CLI session history, with a TUI and print mode.",
  "type": "module",
  "bin": {
    "agent-grep": "./dist/agent-grep.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun run build.ts",
    "typecheck": "bunx tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "claude",
    "claude-code",
    "codex",
    "agent",
    "grep",
    "cli",
    "tui",
    "search",
    "history"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Evgastap/agent-grep.git"
  },
  "bugs": {
    "url": "https://github.com/Evgastap/agent-grep/issues"
  },
  "homepage": "https://github.com/Evgastap/agent-grep#readme",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "ink": "^7.0.0",
    "react": "^19.2.5",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@vscode/ripgrep": "^1.17.1"
  }
}
